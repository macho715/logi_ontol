from __future__ import annotations
from typing import Iterable, Any

def reason(records: Iterable[dict[str, Any]]) -> Iterable[dict[str, Any]]:
    # Placeholder: hook to ontology_reasoning_engine
    for r in records:
        yield r
